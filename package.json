{
  "name": "nestjs-grpc-microservices",
  "version": "1.0.0",
  "description": "GraphQL API Gateway with gRPC back-end microservices built using the NestJS framework.",
  "scripts": {
    "docs:proto-gen": "./scripts/generate-proto-docs.sh",
    "install": "./scripts/install.sh",
    "lint": "./scripts/lint.sh",
    "build": "./scripts/build.sh",
    "docker:build": "docker-compose build",
    "docker:start": "docker-compose up",
    "docker:teardown": "docker-compose down",
    "start": "npm run install && npm run lint && npm run build && npm run docker:build && npm run docker:start"
  },
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/mihalskiy/nestjs-grpc-microservices/issues"
  },
  "homepage": "https://github.com/mihalskiy/nestjs-grpc-microservices#readme"
}
